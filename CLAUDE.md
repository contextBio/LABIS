# LABIS — Lab Intelligence System

연구실 운영(과제·인사·연구비·성과·구매·장비)과 LIMS(시료·실험)를 한 앱에서 다루는 서비스.

이 문서는 **왜 지금 모양이 되었는지**와 **고칠 때 지켜야 할 것**을 남긴다.
설치·실행·릴리스 절차는 [README.md](README.md) 와 [scripts/release.sh](scripts/release.sh) 가
정본이므로 여기서 반복하지 않는다.

## 지금 모양

| 층 | 실체 |
|---|---|
| 화면 | `frontpage/index.html` — 한 장짜리 정적 SPA. 해시 라우팅, 프레임워크·빌드 없음 |
| 백엔드 | Next.js (`src/`) — `basePath=/labis`, Bearer 토큰 REST (`/api/v1/*`) |
| 인증 | contextBio 통합계정 ID 토큰 단일화 (자체 로그인·MUSE SSO 는 걷어냈다) |
| 저장소 | bare + 두 워크트리 — dev(:3101, `labi_dev`) / main(:3100, `labi`). 브랜치를 갈아타지 않는다 |

공개 주소 `contextbio.ai/LABIS` 는 **별개 저장소** `ContextBio/ContextBio` 의 `build_subapp.py`
가 이 SPA 파일을 그대로 싣고 사이트 쉘(상단 줄·푸터·Firebase 브리지)을 씌워 빌드한다.
백엔드는 `c1.sysmed.kr/labis` 로 따로 선다.

## 이력

| 시기 | 국면 | 내용 |
|---|---|---|
| 08-27 | 태동 | SQLite MVP(`536de60`) → Postgres+Prisma·Auth.js·멀티랩 권한 뼈대(`f435a49`), 도메인 모듈 랩 스코프 이식(`2089c5e`), `contextbio.ai` 연결(`0784e9d`) |
| 08-30 | 재편 | 관리 항목 6종 재구성(`0199745`), 폴더 수집 에이전트(`28dde93`), LABi → **LABIS** 리브랜딩(`1829778`), dev/main 분리와 `release.sh`(`c801504`, `f595a71`) |
| 08-31 | 로그인 통합 | 자체 로그인·MUSE 제거, contextBio 계정을 유일한 정규 로그인으로(`86892eb`, `51d5be5`), 공통 상단 바(`b07bf36`) |
| 09-01 | SPA 전환 | 정적 프론트 3단계 포팅 — 조회(`c23f19f`) → 구성원 배정(`bd931f2`) → 등록·수정·삭제(`8219622`). 토큰 릴레이 입장(`0d11ee3`), 항목별 구글시트 연동(`748fd71`), 메뉴별 권한(`f8e4231`) |
| 09-02~03 | 운영 정리 | 관리자 설정 한 곳으로(`968e854`), 연구실 삭제 → **폐쇄** 상태로(`498b53e`), 시트 연동을 설정 안으로(`27fa619`), 셀프서비스 개설(`7c7c561`), 연구실 5개 상한(`2d04d2b`) |
| 09-03~04 | 화면 전환 경합 | 늦게 온 응답이 사라진 DOM 을 건드려 터지던 문제 — 시트·설정부터(`14609bf`), 이어서 목록 화면 전체로(`512c697`) |

릴리스는 main 의 `release: dev 머지 (<dev SHA>)` 머지 커밋과 `release-<날짜>-<시각>` 태그로 남는다.

## 고칠 때 지켜야 할 것

**늦게 온 응답은 끊는다.** 화면을 다시 그리면 이전 화면의 DOM 은 사라진다. 그 뒤 도착한
응답이 `$("t1").innerHTML = …` 을 하면 `Cannot set properties of null` 이 나고, 그 오류가
`showErr` 를 타고 **새로 열린 화면의 `#err` 에 오배송**된다 — 사용자에게는 멀쩡한 화면이
"불러오지 못했습니다" 로 보인다. 그래서 `route()` 가 `RENDER_SEQ` 를 올리고,
화면용 조회는 `fresh(...)` 로 감싸 세대가 지나면 `then`·`catch` 를 아예 타지 않게 한다.
DOM 에 쓸 때는 `setHtml(id, html)` 로 없는 자리를 조용히 넘긴다.
**새 화면이나 새 조회를 추가하면 반드시 `fresh(list(...))` / `fresh(req(...))` 로 감쌀 것.**

**시트·폴더 수집은 전체 교체를 하지 않는다.** 예전에는 9개 항목이
`deleteMany({where:{labId}})` 로 랩의 행을 전부 지우고 다시 넣었다 — 사람이 화면에서 등록한
구매·휴가·논문이 가져오기 한 번에 사라지고, 행 id 도 매번 바뀌어 감사 로그·화면 조작 대상이
어긋났다. 지금은 `sheetSync.ts` 의 `syncRows()` 가 `sourceKey`(행 내용에서 만든 자연키)로
갱신·추가하고, 시트에서 사라진 **시트 기원** 행만 지운다. **새 항목을 붙이거나 `importRows` 를
고칠 때 `deleteMany` 를 쓰지 말 것.** 규칙 표는 [README](README.md#동기화-규칙-시트폴더-공통) 에 있다.

**인덱스는 조회 패턴을 따른다.** 목록 화면은 모두 `where {labId} + orderBy(날짜/이름)` 이므로
`@@index([labId, <정렬키>])` 형태로 둔다. 외래키 컬럼에도 인덱스를 둔다 — Postgres 는 FK 에
자동으로 만들어 주지 않아서, 없으면 부모(과제·사용자) 삭제가 자식 테이블 풀스캔이 된다.

**에이전트 코드는 `server-only` 모듈을 타면 안 된다.** `scripts/` 는 tsx 로 Next 런타임 밖에서
돌아 `guard.ts`(쿠키·리다이렉트) 를 import 하는 순간 죽는다. 감사 로그를 `audit.ts` 로 떼어낸
이유가 이것이다(`guard.ts` 는 재export 만 한다).

**릴리스는 `scripts/release.sh` 로.** 손으로 빌드하지 않는다. 스크립트가 서버를 **먼저**
내리는 것은 구동 중 `npm ci` 가 `node_modules` 를 지우다 `ENOTEMPTY` 로 죽기 때문이고,
빌드가 고쳐 쓰는 `next-env.d.ts`·`tsconfig.json` 은 끝나고 원복한다. `.claude/` 는 세션 도구가
수시로 고치므로 청결 검사에서 뺀다.

**워크트리에 쌓인 파일 권한(mode) 변경이 머지를 막는다.** 내용은 0줄 바뀌었는데
`git merge` 가 "로컬 변경이 덮여쓰인다" 며 거부하면, 설정을 남기지 않는
`git -c core.fileMode=false <명령>` 으로 통과시킨다.

**화면 문구가 배포 후에만 이상하면 사이트 빌더를 의심한다.** `build_subapp.py` 의 경로
재작성이 스크립트 문자열까지 건드려 `3/10개 항목 연결됨` 이 `3/labis/10개` 로 나온 적이
있다(2026-09-05, 저 저장소에서 수정). 저장소 파일과 라이브 HTML 을 `diff` 하면 금방 드러난다.

**서비스 계정 키는 저장소에 두지 않는다.** 히스토리에서 제거한 이력이 있다(`186e87a`).
기본 경로는 `data/service-account.json`, `.gitignore` 로 막혀 있다.
